server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router (BrowserRouter) の client-side routing 用 SPA フォールバック
    location / {
        try_files $uri $uri/ /index.html;
    }
}
